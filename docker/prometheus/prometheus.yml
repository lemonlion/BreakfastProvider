global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'breakfast-provider-api'
    metrics_path: /metrics
    static_configs:
      - targets: ['breakfast-provider-api.local:8080']
        labels:
          environment: 'docker'
