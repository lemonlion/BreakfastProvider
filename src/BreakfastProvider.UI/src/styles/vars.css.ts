import { createVar } from '@vanilla-extract/css';

export const progressWidth = createVar();
export const toastTimerWidth = createVar();
export const toastOffset = createVar();
export const sidebarWidth = createVar();
